Use the siesta.fdf file to run Siesta (you can find the
pseudopotentials needed in Tests/Pseudos).

The wavefunctions will come out in the si2x1h.selected.WFSX file. You have
to rename it (or make a symbolic link) to si2x1h.WFSX before proceeding.

Finally, run the STM program:

stm < stm.fdf

